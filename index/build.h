#pragma once
#include <iostream>
#include <string>
#include "defines.h"
#include "util/defines.h"
#include "../util/defines.h"



template<typename T>
void build_disk_index(const std::string& raw_data_path, const std::string& index_output_path,
                      const std::string& params, MetricType mt = MetricType::L2) {
}
